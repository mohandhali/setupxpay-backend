import { Blockchain } from '@tatumio/shared-core';
import { CancelablePromise, PendingTransaction } from '@tatumio/api-client';
export declare const abstractBlockchainKms: (args: {
    blockchain: Blockchain;
}) => {
    /**
     * Get pending transactions to sign
     * <h4>1 credit per API call.</h4><br/><p>Get list of pending transaction to be signed and broadcast using Tatum KMS.</p>
     * @param signatures Signature IDs of the KMS which invokes this endpoint. If multiple, they should be separated by comma.
     * @returns PendingTransaction OK
     * @throws ApiError
     */
    getAllPending(signatures?: string): CancelablePromise<PendingTransaction[]>;
    get: typeof import("@tatumio/api-client").KeyManagementSystemService.getPendingTransactionToSign;
    complete: typeof import("@tatumio/api-client").KeyManagementSystemService.completePendingSignature;
    delete: typeof import("@tatumio/api-client").KeyManagementSystemService.deletePendingTransactionToSign;
};
