import type { DecodedDataCommon } from './DecodedDataCommon';
export declare type NftTransfer = (DecodedDataCommon & {
    /**
     * The unique identifier of the NFT being transferred.
     */
    tokenId?: string;
});
