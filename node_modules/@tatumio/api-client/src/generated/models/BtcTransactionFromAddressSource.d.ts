export declare type BtcTransactionFromAddressSource = {
    /**
     * The blockchain address to send the assets from
     */
    address: string;
    /**
     * The private key of the address to send the assets from
     */
    privateKey: string;
};
