import type { DecodedDataCommon } from './DecodedDataCommon';
export declare type MultitokenTransfer = (DecodedDataCommon & {
    /**
     * The address of the operator initiating the transfer.
     */
    operator?: string;
});
