import type { Chain } from './Chain';
import type { TokenType } from './TokenType';
export declare type NativeTokenBalance = {
    chain: Chain;
    type: TokenType;
    address: string;
    balance: string;
};
