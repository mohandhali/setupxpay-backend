import { ITronWeb } from './tron.web';
export declare const tronGasPump: (args: {
    tronWeb: ITronWeb;
}) => {
    prepare: {
        gasPumpBatch: (testnet: boolean, body: any, provider?: string) => Promise<string>;
    };
};
