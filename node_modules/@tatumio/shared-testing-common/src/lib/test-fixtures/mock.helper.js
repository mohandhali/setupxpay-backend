"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mockHelper = void 0;
const { ApiError } = jest.requireActual('@tatumio/api-client');
exports.mockHelper = {
    /**
     * Make sure to add
     * jest.mock('@tatumio/api-client')
     */
    mockApi: (client) => {
        return jest.mocked(client.ApiServices, true);
    },
    apiError: {
        notFound: (status = 403, errorCode = 'errorCode') => {
            return new ApiError({
                ok: false,
                status,
                url: 'mocked_url',
                statusText: 'Forbidden',
                body: {
                    statusCode: status,
                    errorCode,
                },
            }, '');
        },
    },
};
//# sourceMappingURL=mock.helper.js.map